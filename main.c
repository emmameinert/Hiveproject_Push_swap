#include <stdio.h>
#include <stdlib.h>

//operation file

//think about how to save the numbers and the stack 

void check_input(char **input)
{
    while (argv[i][j] != '\0')
    {
        if (argv[i][j] )
    }

}
int main(int argc, char **argv)
{
    if (argc < 2)
        return (0);
    //check_input(argv);
    if (argc == 2)
        return (argv[1]);
    return (0);
}
